id = "recovery"
status = "safe-shell-only"
frontend_routes = ["recovery", "power"]
development_provider = "recovery/safe-recovery.sh"
future_runtime = "Signed offline recovery image and verified A/B update integration"
security_note = "No repair, reset, partition, bootloader, or firmware operation exists here."

