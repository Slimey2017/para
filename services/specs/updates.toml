id = "updates"
status = "contract-only"
frontend_routes = []
current_provider = "none"
future_runtime = "Signed atomic updater with rollback and power-loss tolerance"
linux_apis = ["systemd-sysupdate or OSTree", "fwupd as a separate opt-in boundary"]
